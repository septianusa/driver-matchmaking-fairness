# Raw Data Placeholder

Place private or company-approved raw CSV files here when rerunning the simulator.
Do not commit confidential row-level data to a public repository.

Expected files for the full simulator are:

- `orders.csv`
- `driver_locations.csv`
- `driver_scores.csv`
- `grid_values.csv` (optional; header-only is allowed)

